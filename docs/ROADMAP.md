# Roadmap — Fast Lane

> Canonical "what's next" — AI agents and humans both pull work from here (see AGENTS.md).
> Each row is sized to fit one working session. **Size:** S = under an hour, M = one session, L = one long session (consider splitting).

## In Progress / Up Next

> Keep this block tiny: at most 1-2 items in progress, 1-2 up next.

| Status     | Item                        | Notes                                                                |
| ---------- | --------------------------- | -------------------------------------------------------------------- |
| ⬜ up next | Daily challenge (Wave 3, S) | Achievements & stats shipped. Hot-seat multiplayer (L) still pending |

## Wave Sequence

### Wave 0 — Mobile-first foundation ✅ complete (2026-08-24)

iPhone browser/Safari is our eventual core user base — the game should feel built for a phone in your hand first, with desktop as the thing that still happens to work, not the other way around. Every item below shipped; real-device QA (see the last row) caught 2 bugs neither Chromium testing nor code review would have.

> **Current state, checked against the code:** `.game-layout` only collapses its two-column grid at 900px — the `TopBar` above it stays one long flex row of 7 stats plus a headline plus a Menu button, which wraps/overflows badly under ~430px. `.board` is a fixed 4×4 grid with 96px-min tiles sized for a desktop pointer, not a thumb on a 375px-wide screen. `index.html`'s viewport meta has no `viewport-fit=cover`, so `env(safe-area-inset-*)` never activates — nothing accounts for the notch/Dynamic Island or home indicator. There's no `apple-touch-icon`/`theme-color`/`apple-mobile-web-app-capable`, so "Add to Home Screen" looks like a bare bookmark. Buttons across `LocationPanel.tsx` use the default `0.45rem 0.9rem` padding with no min-height — several fall under Apple's 44×44pt touch-target minimum. (Fixed below — this note originally also claimed `:active` was missing; it wasn't, that part was wrong.)
>
> **Non-UI risk worth flagging here too:** `GameContext.tsx` already autosaves to `localStorage` after every action (good), but iOS Safari's Intelligent Tracking Prevention purges localStorage for sites the player hasn't opened in ~7 days — and IndexedDB isn't exempt either. An installed ("Add to Home Screen") PWA _is_ exempt. So Home Screen presence below isn't just a nice icon — for a game people come back to over days/weeks, it's what keeps their save alive at all.
>
> **UI test-coverage note, mirroring Wave 2's below:** `src/ui/__tests__/` has one file (`App.test.tsx`) covering `Board.tsx`, `GameScreen.tsx`, and `LocationPanel.tsx` combined — the exact three components this wave rewrites. Add component tests (React Testing Library, already in the stack) for whatever you touch, so the mobile-first rewrite doesn't ship untested.

| Status        | Item                                   | Size | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------- | -------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅ 2026-08-24 | Responsive layout overhaul             | M/L  | Rebuilt `TopBar` (pinned brand/menu row + horizontally-scrollable stat strip), `Board` (shrunk tiles/panel under 480px, stays ≥64px so it clears the 44pt minimum), and `LocationPanel` (sheet-styled: rounded top, pull-tab affordance) — visually a bottom sheet, not a true draggable/dismissible one; deferred that interaction complexity given Wave 0's remaining items. Verified via Playwright screenshots at 375px and 1280px: zero horizontal overflow, no console errors, desktop unaffected. `index.css`, `GameScreen.tsx`, `Board.tsx`, `LocationPanel.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ✅ 2026-08-24 | iOS safe-area & viewport correctness   | S    | Added `viewport-fit=cover`; `.app`, `.modal-backdrop`, and `.toast` now pad/position with `max(base, env(safe-area-inset-*))` so the notch/Dynamic Island and home indicator won't cover content. Verified no regression via Playwright (Chromium supports `env()`, evaluates to 0 without a real cutout) — actual notch/Dynamic Island behavior still needs the Wave 0 device QA pass on real hardware, since that's the only way to see non-zero inset values.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ✅ 2026-08-24 | Touch targets & input behavior         | S    | Global `button`/`input[type=number\|text]` rules now guarantee `min-height: 44px` and `touch-action: manipulation`; number inputs get explicit `font-size: 16px` (was relying on inherited default) plus `inputMode="numeric"`/`enterKeyHint="done"` on the deposit/withdraw/lottery inputs. `:active` tap states already existed — an earlier audit note above claiming only `:hover` was defined was wrong; corrected here. Verified via Playwright: every non-tile button measures ≥44px, the number input measures exactly 44px/16px.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ✅ 2026-08-24 | Scroll & viewport polish               | S    | `html`/`body` get `overscroll-behavior-y: none` (kills page-level rubber-band chaining); `.log` and `.modal` get `overscroll-behavior: contain` + `-webkit-overflow-scrolling: touch` so their internal scroll doesn't chain to the page; `.topbar-stats` gets the same for its horizontal scroll. Verified zero horizontal overflow at 375px (`scrollWidth === clientWidth`) via Playwright, no console errors.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ✅ 2026-08-24 | Home Screen presence + save durability | S    | Added `public/favicon.svg` + `public/apple-touch-icon.png` (placeholder brand-blue "FL" monogram — swap for real artwork whenever there's a designer pass), `theme-color` (light/dark), `apple-mobile-web-app-capable`/`-status-bar-style`/`-title` meta. New `InstallPrompt.tsx`: iOS-only nudge (no native install-prompt API on iOS Safari) shown after a player's first completed week, dismissible, remembered in localStorage. Verified via Playwright with a spoofed iOS UA: prompt appears after week 1, both icon URLs resolve 200, no console errors.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ✅ 2026-08-24 | Social & SEO meta tags                 | S    | Added Open Graph + Twitter Card tags and a rendered `public/og-image.png` (1200×630, on-brand gradient + wordmark + the four goal chips). URLs updated to `fastlane.andernet.dev` once the domain migrated (see below). A shared link (including Wave 3's future Daily Challenge result) now gets a real title/image preview in iMessage/Twitter instead of a bare URL.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ✅ 2026-08-24 | Migrate to fastlane.andernet.dev       | S/M  | Domain attached in Cloudflare Pages by the user; `jones.andernet.dev` removed from custom domains. Code-side follow-up done: `index.html`'s OG/Twitter URLs updated. **Not done: the old domain returns a Cloudflare 530 instead of redirecting** (`jones.andernet.dev` has no origin now) — anyone with an old link hits an error page. Needs a Bulk Redirect or Worker rule in Cloudflare if that matters; flagged to the user, not fixed here since it's a dashboard action outside this repo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ✅ 2026-08-24 | Save export/import                     | S    | `GameContext.tsx` gained `exportSave`/`importSave` + a loose structural validator. Placement corrected from the original note: **Export** lives in `TopBar` (a 💾 button next to Menu) since a save only exists during active play; **Import** lives on `StartScreen` (file picker) since that screen only ever appears when there's no save to protect. Verified via Playwright: a real export→quit→import round-trip preserves exact state (savings, name), and a garbage JSON file surfaces a clean error instead of crashing. Pulled ahead of the domain migration below since I can execute this one myself and not that one.                                                                                                                                                                                                                                                                                                                                                                                    |
| ✅ 2026-08-24 | Wave 0 device QA pass                  | S    | Closing gate for this wave, done across two rounds on a real iPhone. **Round 1 (dark mode, portrait, Safari tab)** found and fixed 2 real bugs neither Chromium testing nor code review caught: `.goal-row`'s range input had no explicit width and overflowed its `1fr` grid track, clipping goal values off-screen on `StartScreen`; `.side`'s negative `margin-top` (a "pulled up" sheet effect) was covering real tile text, not just its shadow — removed. **Round 2** confirmed landscape clean, and the installed Home Screen app: launches full-screen with no Safari chrome, status bar/home-indicator areas clear of content, icon + "Fast Lane" label correct, install banner correctly hidden once standalone. Light mode wasn't separately re-verified on device after the round-1 fixes (same CSS custom-property system as dark, lower risk, and both rounds' fixes were layout/overflow issues unrelated to color scheme) — reasonable to close given nothing color-scheme-specific was ever flagged. |

### Wave 1 — Game feel & clarity ✅ complete (2026-08-24)

The engine is done; the game _works_ but doesn't _feel_ alive. Framed as "UI-only — no engine changes," but 2 of 5 items (Riley turn playback, End-of-game recap) needed small additive engine changes to deliver what was actually asked for — both explicitly approved before building, both purely additive (new fields/logging, zero rule or AI-decision logic touched).

| Status        | Item                | Size | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------- | ------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅ 2026-08-24 | Riley turn playback | M    | Step-by-step replay of Riley's real week: the pawn walks their actual path, a bottom bar captions each action, Skip jumps straight to the report. **Required a small, deliberately-scoped engine change** (user approved): `actions.ts`'s `log()` now attaches the actor's `location`, and 7 previously-silent actions (`travel`, `buyMeal`, `buyGroceries`, `buyLottery`, `deposit`, `withdraw`, `relax`) now log — purely additive, no rule/AI logic touched. Along the way, found and fixed a real pre-existing bug: `endWeek()`'s `logStart` was captured _after_ `runRileyWeek()` already ran, so `lastReport.entries` — and therefore the static week-report modal, this whole time — never actually contained Riley's turn, only end-of-week system events. Fixed by capturing `logStart` in `engine.ts` before Riley's turn runs. Verified via Playwright: pawn position matches each caption, overlay blocks board clicks during replay, Skip reaches the modal, modal now shows Riley's full real week. `types.ts`, `actions.ts`, `week.ts`, `engine.ts`, `Board.tsx`, `GameScreen.tsx`, `App.tsx`.                                                                                                                                                                                                                                                         |
| ✅ 2026-08-24 | Onboarding + help   | M    | New `Help.tsx`: a "?" button in `TopBar`, always available, opens a modal covering goals, time/board, dress/jobs, savings/economy (interest, robbery risk, eviction), and Riley — content checked against `week.ts`'s actual `upkeep()` for accuracy (dress wear 3/week, eviction at 3 weeks behind, robbery above $400 cash without a secure apartment) rather than guessed. **Two deliberate deviations from the written spec, both improvements:** no true first-run _coach-mark_ tour (spotlight-on-live-elements) — a step-through informational modal covers the same ground far more reliably given the mobile layout work already done, and is what auto-opens on first visit instead; the "seen it" flag lives in `localStorage` (`fastlane-help-seen`), not save data, so it's "has this browser seen the tour" rather than "has this save" — a returning player starting a fresh game won't get re-nagged. Zero engine changes needed. Verified via Playwright: auto-opens once, dismiss works, reopens via the ? button, zero horizontal overflow at 390px, no console errors. Added a new test covering the auto-open/dismiss/reopen cycle.                                                                                                                                                                                                              |
| ✅ 2026-08-24 | Board animations    | M    | The player had no board token at all before this — only a border highlight; added a 🙂 marker to match Riley's 🎩. Both are now single persistent DOM elements moved via a measured `getBoundingClientRect()` position + CSS `transition: transform` (a real glide, GPU-composited, reliable in Safari) instead of remounting inside whichever tile matched — re-measures on location change and on board resize (`ResizeObserver`) so it holds up across breakpoints/orientation. Newly-arrived tiles get a 3× pulse (`box-shadow`, theme-aware via a new `--accent-rgb` token). Cash (`TopBar`) and time (`CenterPanel`) get a floating `+/-N` flash on change (`useDeltaFlash`, exported from `Board.tsx`). All of it respects the existing global `prefers-reduced-motion` override. Verified via Playwright at desktop and 375px: transform genuinely changes on travel (not just class-swapping), pulse animation applies to the arrived tile, delta flash reads correctly (`-1h` on a 1h walk), both markers stay within board bounds with zero horizontal overflow, no console errors.                                                                                                                                                                                                                                                                        |
| ✅ 2026-08-24 | Sound               | S    | New `src/ui/sound.ts`: synthesized WebAudio oscillator blips (move, purchase, payday, disaster, win), no asset files. A 🔊/🔇 toggle in `TopBar` persists to `localStorage`. Move plays on travel; purchase on buying groceries/items/meals/lottery/rent/tuition; payday on working; win on `GameOver` if the player won; disaster is text-matched against `week.ts`'s exact `upkeep()` phrases ("went hungry", "evicted", "robbed") for the player's entries in a new report, not guessed. Verified via Playwright: mute toggle persists and flips the icon, triggering each sound path (travel, apply+work, buy meal, end week) produces zero console errors.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ✅ 2026-08-24 | End-of-game recap   | S    | "Data already in state history" wasn't true (third inaccurate spec found this way this session) — `GameState` had no snapshots, just a text log. **User approved another small additive engine change**, same pattern as Riley turn playback: `WeekSnapshot[]` history recorded once per week in `endWeek()`. Loading/importing an old save without `history` is defaulted to `[]` (`normalizeSave()` in `GameContext.tsx`) rather than crashing. New `RecapChart.tsx` follows the dataviz skill's full procedure: two single-axis line charts (net worth, career — never dual-axis, per the skill's #1 anti-pattern), a `--chart-you`/`--chart-riley` categorical pair validated with `scripts/validate_palette.js` **separately per mode against the real card surface** (light passes with a contrast WARN mitigated by direct end-labels; the naive dark-mode flip of the light pair FAILED the lightness band — re-picked `#cc7a1f`, which passes), crosshair + shared tooltip, a full table-view twin for accessibility, and a collision-avoiding nudge on the end-labels (found via actually screenshotting it — two close final values were rendering on top of each other, illegible). Verified via Playwright across light/dark/mobile/hover/table-toggle and two edge cases (1-week history, zero-history for old saves) — all clean, zero console errors. |

### Wave 2 — Gameplay depth ✅ complete (2026-08-24)

Engine + UI pairs. Keep each mechanic in its own module in `src/engine/` with tests; the deterministic-engine pattern (all randomness via `rng.ts`) must hold.

> **Test-coverage note:** the engine currently has one test file (`engine.test.ts`) covering ~1,100 lines across `actions.ts`/`week.ts`/`ai.ts`. Every item below touches this code — backfill unit tests for the module you're editing as you go, not just the new mechanic, so coverage doesn't stay this thin as gameplay depth grows.
>
> `week.ts`'s `personalEvent()` currently rolls 4 flat outcomes (found cash, doctor bill, work bonus, old friend). Health & doctor, Durable goods (robbery), and Loans & credit (garnishment) each add their own outcome to that table — extend it in place rather than starting a parallel event system.
>
> **Ship the first three rows below before any other item in this wave.** Every remaining row changes `PlayerState`'s shape. Right now there's no save-schema versioning (`GameContext.tsx`'s `SAVE_KEY = 'fastlane-save-v1'` is a hardcoded string nobody's required to bump) and no error boundary anywhere in the app — so a shape change today would silently corrupt an old save with `undefined` fields (which then autosaves right back over the real save) or white-screen the app with no recovery path. Land the safety net first.

| Status        | Item                                | Size | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------- | ----------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ✅ 2026-08-24 | Save schema versioning & migration  | M    | `GameState.version` (engine-owned, stamped by `newGame()`) + a `MIGRATIONS` map in `GameContext.tsx`, run by a new `migrateSave()` on both load and import. **Judgment call, not the literal spec:** rather than treating every pre-versioning save as an unknown shape and wiping it to a fresh game, added an explicit `0 → 1` migration — that shape is fully known (it's everything this app has ever shipped), so it upgrades existing player progress in place instead of discarding it on the very feature meant to prevent silent data loss. Truly unknown saves (corrupted JSON, a version newer than this build understands, missing fields with no migration path) still fall back to fresh — now with a visible error toast explaining why, previously silent. `SAVE_KEY`'s "-v1" suffix is retired as a versioning mechanism (frozen name now, real version lives in the data). `ErrorToast` moved to render on the StartScreen branch too, since a failed-load message fires exactly when there's no game to show it against. New tests: legacy-save upgrade preserves real progress, corrupted JSON recovers with a toast, a from-the-future version resets safely. Verified via Playwright against a real legacy-shaped save (not just unit tests): resumes with correct data, upgrades in storage, and plays forward normally afterward.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ✅ 2026-08-24 | Global error boundary & save reset  | S/M  | `ErrorBoundary.tsx` wraps `<GameProvider><App/></GameProvider>` in `main.tsx` (outside the provider, not inside — so it also survives a `GameProvider`-level failure). Fallback: "Something broke," a "Download my save first" button (reads raw `localStorage` directly, doesn't touch the crashed React tree), and "Reset and start over" (clears the save, hard-reloads). No telemetry hookup yet — that's Wave 5's separate item; this makes a crash survivable, not observable. Verified two ways: a unit test with a component that throws on purpose, and — the real point — a Playwright run injecting a save with an empty `player: {}` (passes `isPlausibleSave`, which only checks top-level shape, but is missing every field the UI reads) to trigger a genuine render crash. Confirmed the fallback shows instead of a blank screen, and Reset clears the save and returns to a working StartScreen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ✅ 2026-08-24 | Baseline balance simulation harness | S/M  | `pnpm sim [gameCount]` (`scripts/sim.ts`, default 200 games) runs headless AI-vs-AI matches on the Standard preset and reports player/Riley win rate, no-winner rate, and average weeks-to-win, warning if more than 10% of games hit the 60-week cap undecided. **Required a small engine refactor to build honestly:** `ai.ts`'s policy was hardcoded to `riley` (`riley(state)` accessor, a fixed `KEY` constant) with no way to drive the player side by the same logic, so it was renamed `runAIWeek(state, key: PlayerKey)` with every helper (`goTo`, `ensureFood`, `ensureHousing`, `pursueCareer`, `studyOnce`, `workShift`, `pursueHappiness`, `bankSurplus`, etc.) threaded through a `get(state, key)` accessor instead — a pure parameterization, zero behavior change, confirmed by the existing seeded-RNG AI tests passing byte-identical before and after. `engine.ts`'s `'endWeek'` case now calls `runAIWeek(draft, 'riley')`; `runAIWeek` is re-exported from `src/engine/index.ts` so the sim script (outside the engine package) can call it for `'player'` too. Added `tsx` as a devDependency — Node's native TS execution can't resolve the engine's extensionless internal imports (`from './actions'`, a Vite/bundler convention), `tsx`'s esbuild-based resolver can. **Baseline result (200 games, Standard goals):** player 65.5%, Riley 34.5%, 0% no-winner, ~23.5 avg weeks to win either side — an even AI-vs-AI matchup (both sides run the identical policy), so the split reflects turn-order/timing asymmetry in `applyAction`'s `'endWeek'` case, not a policy difference; recorded here as the number to watch for drift, not a target to fix. Rerun `pnpm sim` after each remaining Wave 2 item (Health, Promotions, Durable goods, Loans, Casino) lands.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ✅ 2026-08-24 | Health & doctor                     | M    | `PlayerState.health` (0–100, starts full), drained in `week.ts`'s new `healthUpkeep()`: >40h worked in a week costs health past the threshold, and a week fed entirely from cheap groceries (vs. Burger Barn hot meals) costs a flat amount even without going hungry. Below 40 health, happiness takes an extra weekly hit — how it "feeds the Happiness goal" per the spec, rather than being its own fifth goal. `personalEvent()`'s existing outcome table (per the wave's own note to extend it in place) gained a 5th case: below 50 health, a sickness event actually costs 4–11h the _following_ week; above it, the same roll is a no-op flavor line. New `seeDoctor` action heals +35 for $45/3h at a new Clinic location. **Judgment call — the literal "new Clinic location" spec turned out to be a real board layout change, not a data-table row:** the board was a fixed 4×4 grid (`LOOP_SIZE = 12`, one perimeter cell per location); a 13th location needs a 13th cell. Asked before proceeding rather than assuming; confirmed full board location over a Home-menu action or swapping out an existing stop. Grew the grid to 4 cols × 5 rows (taller, not wider — better for a narrow phone) and widened `.board-center` to match; the resulting 14-cell frame has one cell more than locations, deliberately left empty next to the center panel so it reads as the panel's own shape rather than a missing tile (confirmed by screenshot at 375px and 1280px — no overlap, no cut-off text). Clinic appended at `loopIndex: 12` (not inserted mid-loop) to minimize churn — existing locations keep their loopIndex, but `LOOP_SIZE` growing from 12→13 does shift wrap-around travel costs for pairs that used to go "the short way" past the new slot (e.g. home↔rentoffice: 1h → 2h; caught by the existing `travelCost` test, updated with an explanation rather than silently changed). AI gained an `ensureHealth` priority (visit the Clinic below 50 health, same priority tier as housing) so Riley — and the sim's AI-vs-AI player side — don't run their health into the ground. **Save migration 1 → 2:** backfills `health: 100` and the new `hoursWorkedThisWeek: 0` onto both players for any pre-Health save; `SAVE_VERSION` bumped, new migration test alongside the existing 0→1 one. Considered and dropped a separate `hotMealsThisWeek` counter — `p.fed` already tracks exactly that (only `buyMeal` increments it; groceries are applied separately at week's end), so adding a second field would've just duplicated existing state. 5 new engine tests (overwork drain, no-drain-at-exactly-40h, cheap-food drain, doctor heal, doctor error cases) plus the updated migration/travel-cost tests. `pnpm sim` rerun after landing: 64.5%/35.5% player/Riley (was 65.5%/34.5%), 0% no-winner, ~25.5 avg weeks (was ~23.5, AI now spends time on Clinic trips) — no regression, logged as the new baseline to watch.                                                                                  |
| ✅ 2026-08-24 | Job promotions & career ladder      | S/M  | `PlayerState.jobTenureWeeks`/`promotionLevel`, tracked by a new `careerUpkeep()` in `week.ts` (split out alongside `healthUpkeep()` for the same readability reason). Tenure builds by 1 each week the player works ≥1h at their current job ("no no-shows" per the spec); every `PROMOTION_TENURE_WEEKS` (6) of that earns the next of up to `MAX_PROMOTIONS` (3) levels, each worth +15% wage (`wagePerHour` gained an optional `promotionLevel` param) and +4 prestige (`careerScore`, clamped at 100). **Judgment call:** a no-show week resets the clock toward the _next_ promotion, not an already-earned one — losing a real promotion for one missed week felt too punishing and doesn't match how the roadmap phrased it ("performance … no no-shows" reads as a qualifying condition for progress, not a clawback). Switching jobs or quitting resets both fields to 0 — a promotion is tied to the job, not carried between them. No AI changes were needed: `careerScore` already flows into `ai.ts`'s `bestQualifiedJob`/`nextTargetJob` prestige comparisons, so a promoted AI naturally stops chasing jobs it's effectively already caught up to — which is the whole point ("Career can grow without job-hopping"), confirmed by the sim showing Riley reaching the Career goal at the same rate without needing new logic. **Save migration 2 → 3:** backfills `jobTenureWeeks: 0`/`promotionLevel: 0` on any older save; `SAVE_VERSION` → 3, new migration test alongside 0→1 and 1→2. 5 new engine tests (promotes after 6 weeks, wage bump, no-show reset, job-switch reset, quit reset). Verified via Playwright: `WorkAction` shows "$X.XX/h · promoted ×N" and a "Next promotion in N weeks" hint that wraps cleanly at 375px; `JobBoard`'s current-job row reflects the promoted wage/prestige, other listings correctly show the base (unpromoted) rate. `pnpm sim` rerun: 65.0%/35.0% player/Riley (was 64.5%/35.5%), 0% no-winner, ~25.5 avg weeks — no regression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ✅ 2026-08-24 | Durable goods                       | M    | Two new `gadgets`/`bank` items round out the "computer → office track" and "insurance counters robbery" halves of the spec — Gadget City's TV/fridge/console etc. already existed and needed no changes. **Computer** ($380, Gadget City): `JobDef.requiresComputer`, gating Financial Analyst, Branch Manager, and Professor (the senior "office" roles) — `qualifiesFor()` reports `needs a computer` alongside the existing dress/education/experience reasons, and `ai.ts`'s `pursueCareer` buys one when it's the blocking qualification for the AI's next target job, same tier as its existing dress-outfit purchase. **Home Insurance** ($150, First Bank — not Gadget City; a financial product fits the bank thematically and it's a one-line addition to `BankActions`' panel): a new `burglaryUpkeep()` in `week.ts` (split out alongside `healthUpkeep`/`careerUpkeep` for the same readability reason) gives an unsecured, uninsured home an independent 8%/week chance of losing one random durable good — independent of the existing cash-robbery roll, so a broke player can still lose a TV. Owning insurance skips the roll entirely. **No `PlayerState` shape changed** (items are just new `ItemId`s in the existing `items: ItemId[]` array, `requiresComputer` lives on the static `JobDef`) — no save migration needed, `SAVE_VERSION` stays 3. 4 new engine tests (computer-gated job rejection, a brute-forced deterministic seed where an uninsured bike gets stolen on a known week, the same seed with insurance proving it doesn't). Verified via Playwright: Computer listed at Gadget City, Home Insurance listed at First Bank alongside deposit/withdraw, and all three gated jobs show "needs a computer" in their lock reasons. `pnpm sim` rerun: 67.5%/32.5% player/Riley (was 65.0%/35.0%), 0% no-winner, ~27.5 avg weeks (up from ~25.5 — AI now spends time on computer/insurance purchases and hits more job-qualification friction) — no regression.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ✅ 2026-08-24 | Loans & credit                      | M    | `PlayerState.loanBalance`/`loanWeeksBehind`/`creditScore`/`garnished`/`loanPaidThisWeek`, new `takeLoan`/`repayLoan` actions at First Bank, and a `maxLoan(creditScore) = 300 + creditScore × 20` limit (starts at credit 50 → $1,300). A new `loanUpkeep()` in `week.ts` (same split-out pattern as `healthUpkeep`/`careerUpkeep`/`burglaryUpkeep`) accrues 2%/week interest on any balance regardless of payment, and tracks `loanPaidThisWeek` (set by either `repayLoan` or a garnished paycheck) to raise credit +3 on a payment or drop it −8 on a miss. 3 consecutive missed weeks trigger garnishment — `work()` then auto-diverts 30% of each paycheck to the loan until it clears, at which point `garnished` resets itself. **The one non-negotiable part of this item:** `netWorth()` now subtracts `loanBalance` (`cash + savings − loanBalance`) — without that, taking the maximum loan would instantly inflate cash and let a player "win" the Wealth goal by borrowing, which is exactly the exploit a real credit system is supposed to prevent, not enable. Confirmed via Playwright: borrowing $300 raised Cash by $300 but left Net Worth unchanged. **Judgment call — no AI participation:** Riley never takes a loan (same treatment as the lottery, already player-only) — `pnpm sim` confirmed byte-identical results (67.5%/32.5%, same avg weeks) to the run before this item, proving zero behavior change to AI-driven play, exactly as expected since the AI's code path wasn't touched. TopBar's new Loan stat only renders once `loanBalance > 0`, unlike Rent due's always-on row — showing "$0" forever for players who never borrow is pure clutter Rent due doesn't have (rent is near-universal from week one; a loan is opt-in). **Save migration 3 → 4:** backfills all five new fields (credit score to `CREDIT_SCORE_START` (50), everything else to 0/false) on any older save; `SAVE_VERSION` → 4, new migration test alongside 0→1, 1→2, 2→3. 4 new engine tests (borrow-is-a-wash-on-net-worth + limit rejection, weekly interest accrual, payment resets the clock and raises credit, garnishment actually intercepts a paycheck). Verified via Playwright: Bank panel shows "Credit score: 50 · limit $1,300", "No outstanding loan" / "Owe $X" toggles correctly, Borrow/Repay enable at the right thresholds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ✅ 2026-08-24 | Casino                              | S    | Wheel, not blackjack — a single-roll bet (`playCasino`, `CASINO_WIN_CHANCE` 45% × `CASINO_PAYOUT_MULTIPLIER` 2× ≈ −10% house edge) at a new "Lucky Star Casino" location; resolves immediately in the action itself rather than deferred to week's end (`win.lastReport` stays `null` right after playing — verified by test), which is the more natural read of "time cost per play" for an interactive mini-game. **First engine action to call `roll()` directly** — every prior random event lived in `week.ts`'s upkeep/personalEvent/driftEconomy; a synchronous "spin now" mechanic needs `actions.ts` to consume the shared RNG stream itself, still through the same seeded `rng.ts`, so determinism holds. **The 14th board location exactly filled the one ring cell Health & doctor's board resize had deliberately left empty** — no further grid growth needed, `LOOP_SIZE` 13 → 14, and the board now has zero gap for the first time since Wave 2 began. **A real fragility this surfaced, not just a Casino detail:** adding the location shifted a few wrap-around travel costs again (home↔rentoffice: 2h → 3h, `travelCost` test updated), and because `state.rngSeed` is one counter shared by both players, that shift changed how Riley's AI _travels_, which changed how many `roll()` calls Riley's own upkeep consumes some weeks, which desynced the brute-forced deterministic seed the Durable goods burglary tests depended on (the same seed=2 still works, just 2 weeks later than before — updated with an explanation, not silently). Worth knowing for whoever touches `ai.ts` or adds another location next: a brute-forced seed test is only as stable as Riley's decision tree, not as stable as it looks. **Judgment call — no AI participation, same as the lottery and loans:** Riley never gambles. `pnpm sim` shifted more than prior items (67.5%/32.5% → 58.0%/32.5% → 58.0%/42.0%, ~27.5 → ~29.5 avg weeks) with 0% still hitting the cap — traced to the travel-cost graph change above (both AI sides use the identical policy, but their state trajectories diverge over time, so a uniform graph change lands asymmetrically), not a Casino-specific balance problem, since Casino isn't wired into either AI's decisions at all. No `PlayerState` shape changed, no save migration, `SAVE_VERSION` stays 4. 2 new engine tests (bet-limit/location rejection; a deterministic win and a deterministic loss, seeds found by brute force). Verified via Playwright at 375px: the board renders with no visible gap, the panel shows "The Wheel — bet $10, win $20 (45% chance)", and a spin resolves instantly with Cash updating and Net Worth reflecting the net change, zero console errors.                                                                                                                                                                                                                                                                                                  |
| ✅ 2026-08-24 | AI personalities                    | M    | New `AiProfile` interface (`reserveMultiplier`, `extraWorkHours`, `studyEagerly`, `gambles`) and an `AI_PROFILES` record with the four named presets, threaded through every `ai.ts` decision function that used to call the old parameterless `reserve()` — `runAIWeek(state, key, profile = AI_PROFILES.balanced)`. **Balanced reproduces the original single-policy behavior exactly** (all multipliers neutral, no eager study, no gambling) — confirmed by every pre-existing test that exercises Riley's turn still passing unchanged, and `pnpm sim`'s default invocation landing on the identical 58.0%/42.0%, ~29.5-avg-week numbers as the run right before this item. Hustler (leaner reserve, +10 work-hour cap) and Gambler (bets a fifth of genuine surplus at the casino — the one profile that opts back into gambling, since "Gambler" is the point) both measurably underperform Balanced in `pnpm sim` (74/26 and 64.5/35.5 respectively) — Balanced's well-roundedness across all four goals makes it the strongest policy, so pitching a specific profile is a real (if coarse) difficulty choice, not just flavor. **A genuine bug caught by running the sim, not by the unit tests:** Scholar's eager-study check was first placed at the very top of the priority loop (before even `pursueCareer`), and went **0–200** against Balanced — it kept re-entering `studyOnce` every time it had a sliver of spare cash, before ever securing income, spiraling into "eternal student who never builds wealth." Fixed by moving it to the same low-priority tier as `bankSurplus`/`gambleAtCasino` (spends genuine surplus on classes instead of banking it, rather than studying before anything else); confirmed via `pnpm sim scholar` afterward (58.5/41.5, close to Balanced) and a new regression test asserting Scholar keeps its apartment. **This is why "AI-vs-AI sim test per profile" from the spec earned its own CLI arg** (`pnpm sim [gameCount] [profile]`, player side always Balanced) rather than being read as merely a testing nice-to-have — without it, this regression would have shipped invisibly (the 6 new unit tests below don't run anywhere near enough weeks to surface a slow-bleed economic failure; only hundreds of full games did). `StartScreen.tsx` gained a "Riley's playstyle" picker (reusing the existing `.primary`/`.presets` styles rather than new CSS) with a live blurb per profile; `GameState.rileyProfile` is a new game-level field (not per-player — it configures Riley's policy, not a player's progress), requiring **save migration 4 → 5**, defaulting any older save to `'balanced'`. 6 new engine tests (default/stored profile, Hustler-outworks-Balanced and Gambler-visits-casino via brute-forced seeds, Scholar solvency, a smoke test that every profile produces a playable 10-week game). Verified via Playwright: the picker renders and updates its blurb at 375px and 1280px, selecting a profile and starting a game works with zero console errors. |
| ✅ 2026-08-24 | Rule presets                        | S    | New `RulesConfig` (`eventFrequency`, `economyVolatility`, `startingCash`) and a `RULE_PRESETS` record (`data.ts`) for Classic / Brutal / Zen — resolved onto `GameState.rules` at `newGame()` (not a preset name, so a save stays self-contained even if the preset list changes later). **Classic reproduces today's exact hardcoded defaults** (`{1, 1, 200}`) — confirmed by `pnpm sim`'s default invocation landing on the identical 58.0%/42.0% numbers as the run right before this item. `eventFrequency` scales `personalEvent()`'s trigger chance (clamped at 90% so it can never guarantee-fire); `economyVolatility` required restructuring `week.ts`'s `HEADLINES` table from baked-in multiplier closures (`apply: (s) => s.economy.priceIndex *= 1.05`) to plain percentage/point deltas (`priceDelta: 0.05`) that `driftEconomy()` now scales by volatility before applying — the old closure shape had no way to parameterize its own magnitude. `startingCash` threads through `newPlayer()` for both players equally, not just the human, so Brutal/Zen change the whole game's opening conditions symmetrically. `StartScreen.tsx` gained a "Rules" picker (same `.primary`/`.presets` reuse as the AI-personality picker) placed above it, in goals → rules → opponent → start reading order. `GameState.rules` is a new game-level field, requiring **save migration 5 → 6**, defaulting any older save to `RULE_PRESETS.classic`. 4 new engine tests (Classic-is-default, starting cash follows the preset for both players, and — learning from Casino's single-seed fragility lesson — two _aggregated-across-20-seeds_ statistical tests proving Brutal produces more personal events and larger economy swings than Zen, rather than trusting one seed not to be an outlier). Verified via Playwright: picking Brutal and starting a game actually lands the player at $100 cash in the real TopBar, zero console errors. **Wave 2 is now complete** (7/7 items).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

### Wave 3 — Replayability

| Status        | Item                 | Size | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------- | -------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ⬜            | Hot-seat multiplayer | L    | 2–4 human players, turn hand-off screen. Engine already models players as an array; the work is UI flow + save format bump. Split: engine session, UI session.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ✅ 2026-08-24 | Achievements & stats | M    | New `src/stats.ts` — a top-level module (sibling to `engine/`/`ui/`/`state/`) chosen deliberately: lifetime stats persist in `localStorage` independently of any single save, so they survive "Play again" and starting a fresh game, and don't belong inside `GameState`. **Judgment call that kept this item entirely out of the save-migration chain:** achievement conditions (`Debt-Free`, `Down But Not Out` — the spec's "Win without loans"/"Evicted twice, still won", renamed for flavor — plus `First Win`, `High Roller`, `Ivy League`, `Marathon Winner`) are computed by scanning the _completed_ `GameState.log`, which already holds every action for the whole game and is never trimmed, rather than adding new `PlayerState` counters (e.g. an eviction count) that would have meant another `SAVE_VERSION` bump. `recordGameResult()` dedupes on the game's own `rngSeed` so calling it twice for the same finished game is a no-op. **A real bug this caught, not a hypothetical:** `GameOver.tsx`'s recording `useEffect` ran under React StrictMode (the whole app is wrapped in it, `main.tsx`) — StrictMode double-invokes effects in dev, so `recordGameResult()` fired twice; the second call correctly deduped on `rngSeed` and returned an empty "newly unlocked" list, which then overwrote the first (correct) result in component state, silently hiding the achievement banner every time. Caught via the same Playwright-verification habit used all session (a synthetic completed save loaded into `localStorage`, not a live 10-week playthrough) — the banner just didn't render. Fixed with a `useRef` guard so the recording call only actually fires once per mount; a new test wraps the component in `<StrictMode>` directly to exercise the exact failure path, not just assert the end state. Surfaced on `GameOver.tsx` (an amber/gold "🏆 Achievement unlocked" banner per newly-earned achievement, using the `--gold` design token that was defined but unused until now) and `StartScreen.tsx` (a "🏆 Your Record" section — win/loss tally, fastest win, and all six achievements as locked/unlocked badges — hidden entirely until `gamesPlayed > 0` so a brand-new player never sees an empty trophy case). 13 new tests (9 for `stats.ts`'s pure logic, 3 for the `GameOver` integration including the StrictMode regression, all via a dedicated `GameOver.test.tsx`). Verified via Playwright: a synthetic win unlocking 4 of 6 achievements shows the correct banner text, and "Play again" carries the record forward to the start screen with the right badges marked unlocked. |
| ⬜            | Daily challenge      | S    | Seed-of-the-day (date-derived seed, fixed rules), shareable emoji result grid à la Wordle. Deterministic engine makes this nearly free.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |

### Wave 4 — Cloudflare-native online

Each depends on the previous. Uses the existing Pages project; Functions live in `functions/`.

| Status | Item              | Size | Notes                                                                                                                                                      |
| ------ | ----------------- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ⬜     | Leaderboard       | M    | Pages Function + KV: submit daily-challenge score (seed replay-validated server-side — engine runs in the Worker too), top-100 board. Turnstile on submit. |
| ⬜     | Cloud saves       | M    | D1 table keyed by anonymous device ID; sync/restore saves across devices. Needs a wrangler.toml D1 binding + migration.                                    |
| ⬜     | Async multiplayer | L    | Play-by-turn vs a friend via Durable Objects + invite links. Big; plan its own mini-roadmap first (see AGENTS.md architecture step).                       |

### Wave 5 — Platform & quality

Anytime fillers; good for short sessions.

| Status | Item                                     | Size | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ------ | ---------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ⬜     | PWA                                      | S    | Manifest + service worker (vite-plugin-pwa), installable, offline play. Builds on Wave 0's Home Screen meta tags; pairs naturally with cloud saves.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ⬜     | Playwright E2E                           | M    | Smoke: start game → work a shift → end week → save/reload. Wire into CI after the vitest job.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ⬜     | Accessibility                            | M    | Keyboard-only play, focus management in modals, aria-live for week events, contrast pass. Use the a11y review skill.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ⬜     | Perf/Lighthouse                          | S    | Repo already has `lighthouse.yml` — point it at the Pages URL, fix what it flags (code-split the report modal, fonts).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ⬜     | Client-side error telemetry              | M    | No crash reporting exists for the app itself today — `wrangler.toml`'s `[observability]` only covers Cloudflare Functions logs, and `functions/` doesn't exist yet. Add a lightweight error-telemetry sink (e.g. Sentry free tier) so a crash on a real player's phone is visible instead of silent. Wire into Wave 2's error boundary.                                                                                                                                                                                                                                                                                                                       |
| ⬜     | Enforce coverage thresholds in CI        | S    | `vite.config.ts`'s vitest `coverage` block has no `thresholds`, so `ci.yml`'s test step passes regardless of how thin coverage is. Add reasonable thresholds so the test-coverage notes on Wave 0/Wave 2 are a gate, not just a request.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ⬜     | Deploy safety: gate on CI + protect main | S    | **Verified against the live repo, not just the files:** `main` has no branch protection (`gh api .../branches/main/protection` → 404) and `deploy.yml` triggers on `push: main` fully independently of `ci.yml` — nothing today stops a red build (failing lint/type-check/tests) from shipping straight to production. Add a `needs:` dependency (or a required-status-check branch rule) so Deploy can't run ahead of a green Quality job; a minimal post-deploy smoke step (curl the Pages URL) catches a bad deploy immediately instead of a player finding it. Cheap and zero game-risk — worth doing outside the normal pull order whenever convenient. |

## How To Pull Work

1. Check **In Progress / Up Next**. Finish 🟡 items before pulling new work.
2. Otherwise pull the topmost ⬜ from the active wave.
3. Mark it 🟡 in the same commit as the first code change.
4. When shipped: mark ✅ YYYY-MM-DD, promote the next ⬜, update CHANGELOG.md — same commit.
5. Every item lands with tests passing (`pnpm lint && pnpm type-check && pnpm test`) and deploys via push to `main`.

## Done

<!-- Move ✅ rows here periodically to keep the active tables short. -->

- ✅ 2026-08-13 — Core game: engine, Riley AI, React UI, saves, dark mode
- ✅ 2026-08-13 — Cloudflare Pages deploy + CI (originally at jones.andernet.dev)
- ✅ 2026-08-24 — Rebrand: AI rival renamed Jones → Riley across engine/UI/tests/docs; README/AGENTS.md no longer reference the original 1990 title by name (trademark/franchise-association risk raised as the project grows a public user base)
