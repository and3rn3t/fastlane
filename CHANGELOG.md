# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Mobile-first responsive layout (Wave 0): scrollable topbar stat strip, shrunk board tiles under 480px, sheet-styled location panel
- `viewport-fit=cover` + `env(safe-area-inset-*)` padding on `.app`/`.modal-backdrop`/`.toast` (Wave 0) so iPhone notch/Dynamic Island/home indicator never cover content
- Touch-target sizing (Wave 0): 44px minimum on buttons/number inputs, `touch-action: manipulation`, explicit 16px input font-size, `inputMode`/`enterKeyHint` on numeric fields
- Scroll polish (Wave 0): `overscroll-behavior` containment on the event log/modal/topbar-stats scroll regions so they don't chain into page-level rubber-band bounce
- Home Screen icon/meta + iOS install nudge (Wave 0): `public/favicon.svg`, `public/apple-touch-icon.png` (placeholder), theme-color/apple-mobile-web-app meta, and `InstallPrompt.tsx` — an iOS-only "Add to Home Screen" banner shown after a player's first completed week
- Open Graph + Twitter Card tags and a rendered `public/og-image.png` share preview (Wave 0)
- Save export/import (Wave 0): `GameContext.tsx` `exportSave`/`importSave`, an Export button in `TopBar`, an Import file picker on `StartScreen` — manual backup for players who never install to Home Screen
- Riley turn playback (Wave 1): step-by-step replay of Riley's real week — the pawn walks their actual path, a bottom bar captions each action, Skip jumps to the report. Small additive engine change: `actions.ts`'s `log()` now attaches location, and 7 previously-silent actions now log
- Onboarding + help (Wave 1): `Help.tsx` — a "?" button in `TopBar` opens a modal covering goals, time/board, dress/jobs, savings/economy, and Riley; auto-opens once per browser on first visit (`localStorage`, not save data, so a fresh game won't re-nag a returning player)
- Board animations (Wave 1): the player now has a board token (🙂, matching Riley's 🎩) that glides between tiles via a measured, CSS-transitioned position instead of teleporting; newly-arrived tiles pulse; cash/time changes flash a floating `+/-N`
- Sound (Wave 1): `src/ui/sound.ts` — synthesized WebAudio blips (move/purchase/payday/disaster/win), no asset files, with a 🔊/🔇 toggle in `TopBar` persisted to `localStorage`
- End-of-game recap (Wave 1): `RecapChart.tsx` — two week-by-week line charts (net worth, career) on `GameOver`, built per the dataviz skill's full procedure (validated `--chart-you`/`--chart-riley` palette per mode, crosshair+tooltip, table-view twin, collision-avoiding end-labels). `GameState.history: WeekSnapshot[]` recorded once per week in `endWeek()` — **Wave 1 is now complete** (5/5 items; 2 needed small additive engine changes beyond the wave's original "UI-only" framing, both explicitly approved first)
- Save schema versioning & migration (Wave 2, first item): `GameState.version` + a `MIGRATIONS` map in `GameContext.tsx`, run on both load and import. Includes an explicit `0 → 1` migration that upgrades every pre-existing save in place (preserving real player progress) rather than discarding it, since that shape is fully known. Truly unknown saves still fall back to a fresh game, now with a visible error toast instead of silent data loss
- Global error boundary & save reset (Wave 2): `ErrorBoundary.tsx` wraps the app in `main.tsx`; a crash now shows a recoverable "Something broke" screen (download the save first, or reset and start over) instead of unmounting to a blank white page
- Baseline balance simulation harness (Wave 2, last prereq): `pnpm sim [gameCount]` (`scripts/sim.ts`) runs headless AI-vs-AI games on the Standard preset, reporting win rate and average weeks-to-win — rerun after each remaining Wave 2 mechanic to catch a balance regression before a player does. Baseline: 65.5% player / 34.5% Riley win rate, 0% hit the 60-week cap, over 200 games
- Health & doctor (Wave 2, first gameplay item): `PlayerState.health` (0–100), drained by overworking past 40h/week or a week fed entirely from cheap groceries instead of hot meals; low health drags happiness down too. A new sickness outcome in `personalEvent()`'s table can cost 4–11h the following week below 50 health. New Clinic location (`seeDoctor` action, +35 health for $45/3h) — required growing the board from a 4×4 to a 4×5 grid to fit a 13th location, which also shifts a few existing wrap-around travel costs (e.g. home↔rentoffice: 1h → 2h). AI gained an `ensureHealth` priority so Riley (and the sim's AI side) manage their own health. Save schema bumped to version 2, migrating `health`/`hoursWorkedThisWeek` onto any older save
- Job promotions & career ladder (Wave 2): `PlayerState.jobTenureWeeks`/`promotionLevel` — showing up (working ≥1h) at the same job for 6 weeks earns the next of up to 3 promotion levels, each worth +15% wage and +4 prestige, without switching jobs. A no-show week resets progress toward the next promotion, not one already earned; switching or quitting resets both fields. Save schema bumped to version 3, migrating the new fields onto any older save
- Durable goods (Wave 2): new Computer item ($380, Gadget City) unlocks the senior "office" jobs (Financial Analyst, Branch Manager, Professor) via a new `JobDef.requiresComputer`; new Home Insurance item ($150, First Bank) protects durable goods from a new independent burglary roll (8%/week, unsecured + uninsured homes only) that can steal one owned item, separate from the existing cash-robbery event. AI buys a computer when it's the blocking qualification for its next job, and insures its belongings once it has some
- Loans & credit (Wave 2): bank loans (`takeLoan`/`repayLoan` at First Bank) against a 0–100 credit score that sets the borrowing limit; 2%/week interest accrues on any balance, a payment raises credit, a missed week lowers it, and 3 consecutive misses trigger wage garnishment (30% of each paycheck auto-diverted to the loan) until it's paid off. `netWorth()` now subtracts loan balance, so borrowing can't be used to fake progress toward the Wealth goal. Save schema bumped to version 4, migrating the new fields onto any older save
- Casino (Wave 2): a wheel bet (`playCasino`) at a new "Lucky Star Casino" location — resolves immediately, ~10% house edge by design, player-only (Riley never gambles, same as the lottery and loans). Board grew to 14 locations, filling the one gap left over from Health & doctor's earlier resize; a few wrap-around travel costs shifted again as a result (home↔rentoffice: 2h → 3h)
- AI personalities (Wave 2): Riley's strategy weights extracted into four named `AI_PROFILES` (Balanced, Hustler, Scholar, Gambler — the latter is the one profile that now uses the casino), picked via a new "Riley's playstyle" selector on the start screen. Balanced reproduces the original policy exactly, byte-identical in `pnpm sim`; Hustler and Gambler are measurably weaker opponents, a real (if coarse) difficulty choice. `GameState.rileyProfile` is a new game-level field; save schema bumped to version 5, defaulting older saves to Balanced. `pnpm sim` gained a `[profile]` CLI arg to test each one against a fixed Balanced opponent
- Rule presets (Wave 2, last item — **Wave 2 is now complete**): new `RulesConfig` (event frequency, economy volatility, starting cash) and a Classic/Brutal/Zen `RULE_PRESETS` record, picked via a new "Rules" selector on the start screen. Classic reproduces today's exact defaults, byte-identical in `pnpm sim`. `GameState.rules` is a new game-level field; save schema bumped to version 6, defaulting older saves to Classic
- Achievements & stats (Wave 3): new `src/stats.ts` — lifetime win/loss record, fastest win, and six achievements (Debt-Free, Down But Not Out, First Win, High Roller, Ivy League, Marathon Winner), computed from a completed game's own log rather than new save fields, so this needed no save migration. Shown as a "🏆 Achievement unlocked" banner on `GameOver` and a "🏆 Your Record" section on `StartScreen`, persisted in `localStorage` independently of any save. Fixed a real bug caught along the way: React StrictMode double-invoking `GameOver`'s recording effect was silently hiding the achievement banner every time
- Daily challenge (Wave 3): new `src/daily.ts` — a same-for-everyone daily game (date-derived seed, Standard goals, Classic rules, Balanced Riley) via a new "🗓️ Daily Challenge #N" card on `StartScreen`. `GameOver` shows a Wordle-style emoji grid (one row per goal, 5 squares filled to that goal's final progress) with a "📋 Copy result" button for either side's finished game. `GameState.isDailyChallenge` is a new field; save schema bumped to version 7, defaulting older saves to `false`
- Accessibility (Wave 5, closes the wave — 7/7 items): enabled `eslint-plugin-jsx-a11y`'s recommended ruleset (installed but inert until now) and fixed everything it flagged — the error toast is now a real dismissible button instead of an invalid interactive `role="alert"` div, and the Help backdrop's click-to-close no longer needs a second stopPropagation handler. New shared `src/ui/useModalDialog.ts` gives `Help` and `WeekReportModal` real focus management: focus moves in on open, Tab is trapped inside, Escape closes, focus returns to the trigger. `GameScreen.tsx`'s event log announces just the newest entry via a visually-hidden `aria-live` region, not the whole re-ordering list. Retuned `--accent` in both themes (light `#4f6df5→#4968f5`, dark `#6d87ff→#4566ff`) after a real Lighthouse audit showed both failing white-text button contrast; added a `<main>` landmark to each top-level screen and a global `:focus-visible` outline. Re-running Lighthouse's accessibility category against the fixed build: 100/100
- Playwright E2E smoke test (Wave 5): `e2e/smoke.spec.ts` — start a new game, work a shift, end the week, reload, confirm the save resumes instead of dropping back to the start screen. New parallel `e2e` job in `ci.yml`. Caught a real browser-only bug while writing it: the post-work log text lives inside a native `<details>` collapsed by default, which jsdom (every other test here) doesn't enforce but a real browser does — fixed by opening it first, same as a real player would
- Client-side error telemetry (Wave 5): new `functions/api/log-error.ts` — the repo's first Cloudflare Pages Function — logs client-reported crashes visibly in Workers Logs (`wrangler.toml`'s already-enabled `[observability]`), no third-party signup needed. `src/telemetry.ts`'s `reportError()` posts from `ErrorBoundary.tsx` and new top-level `window.addEventListener('error'/'unhandledrejection')` handlers in `main.tsx`, capped at 5 reports/session, never throws. Added `wrangler` + `@cloudflare/workers-types` as devDependencies and a `functions/tsconfig.json` so this and future Functions (Wave 4) actually type-check and run locally via `pnpm cf:dev`
- PWA / offline play (Wave 5): `vite-plugin-pwa` generates a service worker that precaches the build output, so a returning player with no network still gets the app instead of a blank page. Keeps the existing hand-maintained `public/manifest.webmanifest` (Wave 0) rather than letting the plugin generate a second one. Verified with a headless-browser run: the service worker reaches `activated`, and reloading fully offline still renders the app with zero console errors
- Perf/Lighthouse (Wave 5): `lighthouse.yml` now audits `https://fastlane.andernet.dev` (was a disabled no-op). A real run found ~53KB of unused JS from `GameOver`/`RecapChart` and `WeekReportModal` being bundled eagerly — both are now `React.lazy`-loaded, verified via a real build (split into their own 5.84KB/1.13KB chunks) and a headless-browser Playwright check with zero console errors. Also fixed a second bug the same audit caught: `/robots.txt` had no matching static file, so it fell through to the SPA's `index.html` and got mangled with Cloudflare's auto-injected robots block into 43 parse errors — added a real `public/robots.txt`. Color-contrast/landmark findings from the same audit are intentionally left for the dedicated Accessibility item below
- Deploy safety: post-deploy smoke check + branch protection (Wave 5): `deploy.yml` gained a `smoke-test` job (`needs: deploy`, production pushes only) that curls `https://fastlane.andernet.dev/` with retries so a broken deploy is caught immediately. `main` now has GitHub branch protection: the `Lint, Type-check & Test` CI check is required before a PR can merge, and force-pushes/branch deletion are disallowed. (The `deploy: needs: quality` gate itself turned out to already be shipped from an earlier session — this item only needed the smoke check and the branch-protection rule.)
- Coverage thresholds enforced in CI (Wave 5): `vite.config.ts`'s vitest `coverage` block now sets global `statements`/`functions`/`lines: 75` and `branches: 80` — a few points below the current real numbers (~78.7%/80.4%/78.7%/86.5%) so it's a real gate against regression without demanding an immediate test backfill. `ci.yml`'s existing `pnpm test:coverage` step now actually fails the build if coverage drops below these
- Riley rivalry memory (Wave 6): new `src/rivalry.ts` tracks a win/loss streak across playthroughs in `localStorage`, independent of any save. A losing streak nudges Riley to play slightly more urgently via a new, bounded `ai.ts` `applyMomentum()` (never punishes the player for winning); `StartScreen`/`GameOver` show a one-line flavor reaction. New `GameState.rileyMomentum` field; save schema bumped to version 10. Deliberately excluded from the Daily Challenge, which stays identical for every player
- Legacy perks (Wave 6): new `src/legacy.ts` — permanent bonuses ("Head Start" +$50 starting cash at 3 games played, "New Look" alternate pawn glyph at 1 win) derived purely from existing lifetime stats, no new storage or save migration needed. `engine.ts`'s `NewGameOptions` gained an optional player-only `playerCashBonus`; `StartScreen` gained a "🎖️ Legacy Perks" section reusing the achievement-badge UI
- Next-step hint bar (Wave 11): a subtle, dismissible one-line suggestion on the game screen for when it's unclear what to do next. Reuses Riley's own utility-scored decision logic instead of a second heuristic — `ai.ts`'s candidates each gained a `tag` (purely additive), and a new `previewNextAction()` runs the same ranked-candidate loop `runAIWeek` uses against a throwaway clone (only the previewed player is cloned, mirroring `applyAction`'s own "clone only what's mutated" rule), so the real game state is never touched. No `PlayerState`/`GameState` shape change, no save migration. Along the way, fixed a latent gap in the App test suite's synthetic "v9" save fixture, which claimed to be a fully-migrated save while actually missing every field migrations 1–9 add — harmless before since nothing touched it that early in rendering, but a real crash risk once this hint bar started cloning player state on every render. A Copilot review on PR #19 caught a real accuracy bug: `pursueCareer()` has 5 distinct success paths but originally reported the same generic `'career'` tag for all of them, so the hint could point at the Job Board when the actual move was elsewhere — added a `'career-prep'` tag for the 4 blocker-clearing branches, plus an `App.test.tsx` regression test for the hint's render/dismiss/update cycle
- Sim: rules-preset matrix (Wave 13): `scripts/sim.ts` previously hardcoded the Classic rules preset even though `newGame()` already accepted a `RulesConfig` (Wave 2's Rule presets item) — nothing threaded a non-Classic choice through until now. `pnpm sim [gameCount] [profile] [rulesPreset]` takes a new 3rd optional positional arg (defaults to `classic`, fully backward compatible), and a new `pnpm sim [gameCount] matrix` mode runs the full `AI_PROFILES × RULE_PRESETS` grid (4×3 = 12 cells) as a compact table, flagging any cell whose player win rate drifts more than 10 points from the balanced/classic baseline or whose no-winner rate exceeds 3%. Verified live: rules threading produces genuinely different results per preset (not a no-op), and the matrix's flagging correctly reproduced Wave 2's already-known finding that Gambler and Hustler are measurably weaker than Balanced
- PWA: update-available UX, real manifest, install CTA (Wave 11): `registerType` switched `'autoUpdate'` → `'prompt'` — a new deploy no longer silently swaps the service worker under an already-open tab; a new `UpdateToast` (`virtual:pwa-register/react`) tells the player and asks before reloading instead. `public/manifest.webmanifest` gained `id`/`categories`/`orientation`, a real 512px icon + a proper full-bleed maskable icon (rendered from the existing brand SVG via `sips`), live gameplay screenshots for the install-UI preview, and a Daily Challenge shortcut backed by a small new deep-link feature (`StartScreen.tsx` reads `?daily=1` and auto-starts it). `InstallPrompt.tsx` now shows a branded "Install" button wired to a captured `beforeinstallprompt` event on Android/desktop Chrome, alongside the existing iOS instructions. Found and fixed the same real bug twice: `workbox.globPatterns` sweeps the _entire_ build output for precaching regardless of `includeAssets`, so `og-image.png` (and, newly, the manifest icons/screenshots) needed `globIgnores`, not an `includeAssets` edit — confirmed by inspecting the actual generated `dist/sw.js`, not assumed. Also caught and fixed a real WCAG contrast failure (computed by hand, not guessed: 3.38:1) in the update toast's hover state, and a real regression the test suite caught (`isStandalone()` crashing on environments without `window.matchMedia`, now handled gracefully). Verified everything live against a real `pnpm build` + `pnpm preview`: manifest parses correctly, service worker registers and survives an offline reload, the branded install button actually calls `prompt()`, the daily-challenge deep link auto-starts and cleans the URL — zero console errors throughout
- Working quality CI (`ci.yml`): lint, type-check, format check, tests, build on Node 24 (Python variant included)
- `codeql.yml` and `dependency-review.yml` security workflows
- `.nvmrc` (Node 24), `renovate.json` (canonical config), `commitlint.config.mjs` + `.husky/commit-msg`
- `.cursor/rules/project.mdc` (modern Cursor format) and `.pre-commit-config.yaml` (Python profile)
- `docs/README-template.md` — standard README structure (pitch → badges → features → stack → quick start → scripts)
- Full git-hook suite: `pre-commit` (lint-staged + gitleaks) and `pre-push` (validate gate) + `.lintstagedrc.json`
- Workflow extras: release-drafter (+ config), stale.yml, PR labeler (+ config), Lighthouse starter (disabled)
- `profiles/web/` — canonical eslint/prettier/tsconfig/wrangler configs for React+TS+Vite+Cloudflare repos
- Doc scaffolds: `docs/ARCHITECTURE-template.md`, `docs/PRD-template.md`, `docs/ROADMAP-template.md` (pull-loop convention), `docs/adr/0000-template.md`
- `.gitattributes` — LF normalization, binary marks, lockfiles marked linguist-generated
- Origin backgrounds (Wave 14): 5 starting-background `OriginId`s (`career-changer` neutral, `first-gen-student`, `trust-fund-kid`, `veteran`, `small-town-transplant`), each a `cash`/`education`/`skills` delta plus an inert `traitId` for a future Traits row. `newGame()` applies the human player's (default neutral) and draws Riley's own at random, seeded — the very first RNG draw at construction time. `PlayerState.originId`; save schema bumped to version 11, backfilling `career-changer` on older saves (a real fact, not a guess — that's the exact no-delta stats those saves already have). New `pnpm sim [gameCount] origins` mode (and a matching CI-report section) checks no origin drifts the win rate past the existing guard; caught and fixed a genuinely overpowered first pass (one origin was winning Riley 86% of games) before shipping. Fixed a real regression this uncovered: the Daily Challenge deep-link's "already today's challenge" check compared a save's `rngSeed` straight against the raw date-derived seed, which broke the instant construction started consuming one RNG draw — new `initialRngSeed()` export fixes the comparison
- Origin picker UI (Wave 14): a "Your background" section in `StartScreen.tsx`'s "Customize match" disclosure, matching the existing Riley playstyle/difficulty sections exactly (toggle buttons + a blurb, no new CSS). Threads the selection into `startGame()`'s `playerOriginId`; deliberately excluded from the Daily Challenge button, same as every other customization it already excludes

### Changed

- `docs/ROADMAP.md` restructured and split (240KB → 80KB). Shipped rows' full engineering postmortems moved to a new `docs/ROADMAP-ARCHIVE.md` (all 59 of them, with anchor links from each row) so the roadmap reads as the scannable "what's next" it claims to be. Scattered per-wave rules (no-multiplayer, no-new-gambling, board budget, save migrations, sim discipline, and the Wave 7 RNG-draw-count fragility note) consolidated into one **Standing Constraints** section. Stale state corrected: Wave 7's shipped `personalEvent()` row was still sitting in "Up Next"; Waves 3/6/7/13 were complete but unmarked; Waves 8 and 9 still listed the `ActionRow` primitive as a pending dependency after it shipped 2026-09-04. **One correction against the code, not just the docs:** the board-resize rule said `LocationId` was "proven safe to grow," but `Board.tsx`'s `PERIMETER` is a hardcoded 14-entry array that already uses every cell of its 4×5 frame — the next location needs a new grid shape, which now blocks Wave 8's Pets and Wave 12's New career path until it is made data-driven
- `docs/ROADMAP.md` gained Waves 14–19 (Origins & Traits, The Living City, Body & Mind, Credentials, Reputation & Social Capital, Life Stages & Legacy) — 33 new pending rows scoped against the current engine — plus a **Parked Ideas** table recording what was considered and cut (both multiplayer variants, two rejected gambling mechanics, time-of-day scheduling, content packs) so they stop being re-proposed
- New `src/ui/ActionRow.tsx` de-duplicates `LocationPanel.tsx`'s repeated action-row shell (~13 rows across 9 components) behind one `ActionRow` primitive, plus `Stepper`/`ActionGroup`/`CollapsibleActionGroup` (moved) and a new shared `NumberField` (replacing all 6 raw amount-input blocks). **Correction, same date, from Copilot PR review:** the original version of this entry claimed clearing a `Bank`/`Loan`/`Casino` amount field produced `NaN` (with Casino's Spin button allegedly left enabled showing "bet \$NaN") — that was factually wrong. `Number('')` is `0`, not `NaN`, and a native `<input type="number">`'s value-sanitization algorithm (confirmed empirically, including against jsdom) already guarantees `e.target.value` is either a valid finite numeric string or `''` before any handler runs, so the described failure was never reachable. `NumberField`'s `Number.isFinite` guard — copied forward from `InvestActions`'s pre-existing helper — was itself unreachable dead code and has been removed; this is a pure consolidation of 6 already-correct inline implementations into one shared one, not a bug fix. Bundled the separate "Stepper touch targets" fix in the same change (40×40px → 44×44px) since `Stepper` was relocating anyway, and unified `StartScreen.tsx`'s hand-rolled "Customize match" disclosure into the same shared `CollapsibleActionGroup` it was already a near-duplicate of. `JobBoard` (its own `.job-listing` shape) was left untouched. Also fixed, same review: `ActionRow`'s `children` prop is now optional (a future row with no trailing control would otherwise fail type-checking), and the new tests assert direct-child DOM order instead of just presence-anywhere-in-the-document. 225/225 tests passing.
- The mobile location drawer (peek/expanded `LocationSheet` overlay) is retired after four rounds of behavior fixes (#12/#22/#23/#24, the three "Fixed" entries below) kept trading one trapped/covered-control bug for another — the root cause was a variable-height fixed overlay competing with the board behind it. Replaced with two unambiguous pieces: a slim fixed-height **location dock** (location + the persistent End Week button, deterministic clearance via `--dock-height` instead of the old 7rem guess) and a **real modal actions sheet** reusing the `.modal` + `useModalDialog` pattern Help/WeekReportModal already use — so the location sheet now gets the focus trap, Escape, body-scroll lock, backdrop dismiss, slide-up animation, and top-of-everything z-order for free. Arriving somewhere new still auto-opens it; same-location actions still keep it open. Desktop keeps its static side-column card, now with zero toggle state. Also unified the three inconsistent mobile breakpoints (JS `<900` / CSS `899.98px` / CSS `900px`) into one `(max-width: 899.98px)` query shared by JS (`matchMedia`, which also catches orientation changes) and CSS, and moved `.app`/`.modal` heights from `vh` to `dvh` so iOS Safari's dynamic toolbar can't overhang them. New `mobile-chromium` Playwright project (375×667) with a location-sheet spec, since the old overlay's coverage bugs were invisible to jsdom. Copilot PR review caught two follow-on gaps in this same design before merge: a location with zero enabled action buttons left `useModalDialog` with nothing to focus/trap Tab around and no discoverable dismiss path (fixed with an always-enabled Close button in the dialog header), and the full-viewport dialog (z-index 13) covered the dock's End Week button (z-index 6) at the exact moment travel auto-opened it (fixed with a sticky End Week footer inside the dialog itself).
- Renamed the AI rival from Jones to Riley across the engine (`PlayerKey`, `GameState.riley`, `ai.ts`), UI, and tests; README/AGENTS.md no longer name the 1990 title this project was inspired by. Reduces trademark/franchise-association exposure as the project grows a public user base.
- `docs/ROADMAP.md` gained Waves 21–23 (Mobile Layout & Density, Visual Design Refresh, Mobile Interaction Patterns) after driving the actual mobile experience live on an iPhone-13-sized viewport rather than just re-reading the code — found and fixed one real, screenshot-verified layout bug (the location-modal's sticky footer overlaps the last item of a long scrollable panel; see the Fixed entry below), corrected an initial theory about the board's bottom row that live testing disproved (it already clears the fixed dock on scroll — the real issue is hint-bar content stacking above it on a fresh game, not a missing-clearance bug), and scoped rows for information density, visual polish, and more speculative interaction-pattern bets (a bottom tab bar, swipe gestures) that need an explicit design decision before implementation
- A follow-up mobile audit added two more root-caused Wave 21 rows: `ActionRow`'s `flex-wrap` layout produces genuinely inconsistent wrapping between structurally-identical rows purely from content length crossing the wrap threshold (confirmed via direct DOM measurement at Rent Office's two apartment options, not just a screenshot impression — affects all ~13 `ActionRow` usages), and every location dialog's header subtitle silently truncates to one line with no way to read the rest. Also ruled out and recorded a suspected WeekReportModal/board overlap as a `fullPage` Playwright screenshot-stitching artifact in the audit script itself, not a real bug, after it appeared in two independent screenshots and warranted checking properly rather than assuming
- Live domain migrated to `fastlane.andernet.dev` (from `jones.andernet.dev`), completing the rebrand above; `index.html`'s OG/Twitter URLs updated to match. The old domain has no redirect set up — it currently 530s instead of forwarding.
- `AGENTS.md` is now the single source of truth; `CLAUDE.md`, `copilot-instructions.md`, `.cursorrules`, `.clinerules`, `.windsurfrules` are pointers
- `AGENTS.md` documents and3rn3t stack defaults (React+TS+Vite+Wrangler / Python CLI profiles)
- SECURITY.md, CODEOWNERS, LICENSE filled with real defaults instead of TODO placeholders
- devcontainer Node feature and release workflow action versions updated
- `ai.ts`'s AI policy parameterized from a hardcoded `riley` accessor to `runAIWeek(state, key: PlayerKey)`, so `scripts/sim.ts` can drive either side with the same logic; pure refactor, verified behavior-neutral against the existing seeded-RNG AI tests
- Removed three dead `Icon.tsx` exports (`ListIcon`, `CalendarIcon`, `ChevronRightIcon`) with zero call sites anywhere in `src/`, dating back to the original Aug 28 HIG redesign (Wave 20)
- Extracted `HintBar`/`JobSwitchNudge` (plus their supporting copy tables) out of `GameScreen.tsx` into a new `src/ui/HintBar.tsx` (Wave 20) — pure move, no behavior change. `GameScreen.tsx` had grown to 5 components + 3 hooks in one file since Wave 11 explicitly flagged this exact bundling as a risk to revisit
- Data-driven board geometry (Wave 15, first item): `Board.tsx`'s hand-listed 14-entry `PERIMETER` array replaced with `perimeterForSize(LOOP_SIZE)`, which computes the smallest grid whose border fits every location and walks it clockwise. Pure refactor — no gameplay change, no save migration. One historical wrinkle preserved on purpose: Casino, added as the 14th location without renumbering the other 13 `loopIndex` values, occupies the one grid cell a 13-cell walk had left empty rather than its position in a pure walk's sequence — a new test pins the derived output equal to the old literal array, and a live screenshot confirmed the board renders byte-identically, Casino included. Retires the "hardcoded 14-entry array" framing in `docs/ROADMAP.md`'s board-locations Standing Constraint, unblocking Wave 8's Pets and Wave 12's New career path once a bigger grid shape is chosen

### Removed

- `.github/dependabot.yml` (Renovate is the standard)

### Fixed

- Real-device Wave 0 QA (iPhone, dark mode, portrait, Safari tab) found two mobile bugs neither Chromium testing nor code review caught: `.goal-row`'s range input had no explicit width and overflowed its grid track, clipping goal values off-screen on `StartScreen` (never tested at mobile width before); `.side`'s negative `margin-top` (added for a "pulled up" sheet look) was covering the bottom board-tile row's text, not just its shadow. Also tightened `.start`'s desktop-era spacing for mobile and added a fade-mask on the topbar stat strip so it reads as scrollable instead of cut off. Round 2 (landscape + installed Home Screen app) came back clean — **Wave 0 is complete.**
- `endWeek()`'s `logStart` was captured after `runRileyWeek()` had already run, so `lastReport.entries` — and the static week-report modal — never actually contained Riley's turn, only end-of-week system events. Found while building turn playback; now captured before Riley's turn runs.
- Expanded `LocationSheet` could cover controls behind it (e.g. Home's blurb over the board's End Week button) on short mobile viewports, with no way back except finding the small handle — reported live at Home but confirmed the same across other locations too. Added a `.location-sheet-backdrop` tap-to-dismiss overlay, the same pattern `.modal-backdrop` already uses for Help/the week report, so the expanded sheet now behaves consistently (dismissible via backdrop tap or the handle) across every screen size instead of depending on device height. Desktop's static always-expanded card is unaffected — the backdrop is gated behind an explicit `window.innerWidth` check, not just CSS.
- `LocationSheet` reworked after user feedback that the backdrop fix above still felt like "fighting" the UI: repeated actions at one location (buying groceries multiple times at MegaMart, working several shifts at the Factory) forced a manual re-expand every time because the sheet auto-collapsed to peek after each action — and some locations (MegaMart included) had no peek action at all, so it collapsed into a dead, un-actionable state. That auto-collapse is now removed entirely; the sheet only auto-expands when arriving somewhere new and otherwise stays exactly as the player left it. In its place, a new persistent `.location-sheet-end-week` button lives in the sheet's own handle row on mobile — always reachable whether the sheet is peeked or expanded — replacing the board's own End Week button (now hidden under 900px, since it can sit beneath the sheet's fixed overlay) as the guaranteed way to end the week. Verified live: multiple grocery purchases and work shifts in a row with no forced re-expand, and End Week clickable (confirmed via `elementFromPoint`, and that it actually triggers the report/replay flow) at every step, including after working every available hour.
- The auto-collapse removal above traded one bug for another, reported live after a few week cycles: manually tapping the handle to collapse the sheet at a location with no peek action (First Bank, Gadget City, the Rent Office, ...) dropped it into a dead peek state with nothing but the handle and End Week — no way to deposit, withdraw, apply for a job, etc. without re-expanding (confirmed live: 142px tall vs. 587px expanded, zero controls). A first attempt forced the sheet permanently `expanded` at these locations — caught by review before merging: an always-expanded sheet is a fixed, tall overlay that can cover board tiles on a short viewport, and since neither the handle nor the backdrop tap could shrink it back down anymore, it trapped the player behind it with no way to travel elsewhere except ending the week. The actual fix keeps peek genuinely collapsible everywhere and instead gives it a non-empty fallback ("Tap above to see what's here.") wherever there's no dedicated peek action, so it reads as "more available" instead of "broken" without ever blocking navigation.
- `--good`/`--bad` (`src/index.css`) had no dark-mode override at all, and the light-mode values were themselves borderline — independently recomputed contrast found `--good` 3.20:1 against the light card and `--bad` 3.68:1/4.38:1 against dark/light, all failing WCAG AA's 4.5:1 (Wave 20). Not caught by any prior automated pass since Lighthouse never triggers a low-stat TopBar value or an unmet job-requirement chip on a fresh game. Retuned per-mode, same "re-pick, don't flip" approach as Wave 11's `--gold` fix; `--warn` checked and confirmed clean (only ever a decorative bar background, never text)
- `Help.tsx`'s "Dress & jobs" section never explained the Computer/skill job requirements or the qualify-nudge banner, both real Wave 12 mechanics a new player would hit with zero in-game explanation (Wave 20)
- `.location-modal-footer` (the mobile "End week" bar inside a location's action sheet) was `position: sticky` inside the same scrolling flex column as the panel's own content with no compensating bottom padding, so a long panel's last item (a job listing, the Bank panel's Investing controls) could scroll to a position genuinely obscured underneath it (Wave 21). Found by actually driving the mobile UI live rather than re-reading the code. Fixed by splitting `.location-modal` into a non-scrolling header/footer around a `flex:1; overflow-y:auto` body — the standard non-overlapping sticky-footer pattern
- `ActionRow`'s `flex-wrap` layout made whether the trailing control shared the label's line depend on whether their combined unwrapped widths happened to fit the row, not on any real content rule (Wave 21). At Rent Office, "Basic apartment" (fits) squeezed its label to ~90px and wrapped the title across 2-3 lines, while the structurally identical "Secure apartment" (doesn't fit) wrapped the button below and rendered cleanly — confirmed via direct DOM measurement, not just a screenshot impression. Fixed with a `min-width: 10rem` guard on `.action-row .grow`, forcing the control to wrap below whenever there isn't genuinely enough room for both
- Every location dialog's header subtitle silently truncated to one line with no way to read the rest (confirmed live on Casino and Clinic, neither of which repeats the cut-off text elsewhere in the panel) (Wave 21). Scoped a 2-line `-webkit-line-clamp` fix to `.location-modal-header` specifically, leaving the mobile dock trigger's copy of the same heading single-line — the dock is a fixed-height row that would otherwise grow past `--dock-height` and reintroduce the sticky-footer bug class fixed above
- `JobBoard` rendered every job at every tier for every workplace as one flat list, giving a job the player was nowhere near qualifying for the same visual weight as one they could take right now (Wave 21). Grouped by workplace via `ActionGroup`, the same section-header component already shipped at City University; dropped the now-redundant "· Workplace" suffix from each job's own title since the group header already says it. Tier-collapse (hiding jobs more than one step above the player's current one) is deferred pending its own design pass on what "current tier" means per ladder
- A fresh game's food hint could wrap to 3 lines, and stacked with the qualify-nudge banner, pushed the board far enough down that its bottom row sat partially under the fixed mobile dock (Wave 21). A first attempt — suppressing the qualify-nudge whenever the hint bar had anything to show — was reverted after it broke two tests encoding a deliberate Wave 12 decision that both banners should surface together, not one replacing the other; the design conflict was raised for a decision rather than resolved unilaterally. Shipped instead: both banners still show, each capped to 2 lines via `-webkit-line-clamp` (not 1 — most hint copy runs 60-90+ characters, long enough that a 1-line clamp would routinely cut off the actionable half of the sentence)
- "Customize match" was ~3 phone-screens of scrolling before reaching Import save/Legacy Perks below it (Wave 21). Compared a new segmented-tab pattern against nesting the existing `CollapsibleActionGroup` disclosure (already shipped at Bank's Loans panel) before building either — went with the latter, reuse over invention. "Your background", "Rules", "Riley's playstyle", and "Riley's difficulty" now collapse by default, each showing the current selection in its summary so collapsing doesn't hide what's chosen; collapsed height measured 360px live, down from ~2400px fully expanded

## [0.1.0] - YYYY-MM-DD

### Added

- Initial project setup
- TODO: Describe what was included in the first release

<!-- Link definitions — update these URLs for your repository -->
<!-- [Unreleased]: https://github.com/YOUR_ORG/YOUR_REPO/compare/v0.1.0...HEAD -->
<!-- [0.1.0]: https://github.com/YOUR_ORG/YOUR_REPO/releases/tag/v0.1.0 -->
